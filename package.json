{
  "name": "code-token",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ code-token",
    "create-local": "graph create --node http://localhost:8020/ code-token",
    "remove-local": "graph remove --node http://localhost:8020/ code-token",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 code-token"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
